# Dicta Configuration

# API Key is now stored securely via the Settings interface
# Configure your OpenAI API key in Settings > Model tab
# The key is encrypted using OS-level security (Keychain/DPAPI/libsecret)
# and stored in userData/config/credentials.json

# No environment variables are required for API key configuration
